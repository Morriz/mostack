# using hostNetwork hack until hostPort works with CNI
# @todo: remove and enable localproxy when ready
hostNetwork: {{HAS_CNI}}
localproxy:
  enabled: {{NOT_HAS_CNI}}
  port: 6000
service:
  http:
    externalPort: 6000
proxy:
  enabled: true
persistentVolume:
  existingClaim: slow-pvc
  subPath: registry-cache
