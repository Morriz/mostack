kibanaPassword: {{KIBANA_PASSWORD}}
hostname: kibana.{{CLUSTER_HOST}}
tls: true
containersLocation: /var/lib/docker/containers
service:
  annotations:
    kubernetes.io/ingress.class: {{INGRESS_CLASS}}
