rbac:
  create: false
alertmanager:
  persistentVolume:
    existingClaim: mostack-pvc
    size: 1Gi
    subPath: prometheus-alertmanager
server:
  persistentVolume:
    enabled: false
    existingClaim: mostack-pvc
    size: 1Gi
    subPath: prometheus-server
